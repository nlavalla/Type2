<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="actionBarTheme">@android:style/Theme.NoTitleBar</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:buttonStyle">@style/newbuttonstyle</item>
    </style>
    <style name="newbuttonstyle" parent="@android:style/Widget.Button">
        <item name="android:backgroundTint">#6E6E6E</item>
    </style>

</resources>
